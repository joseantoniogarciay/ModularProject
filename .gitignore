# Tuist
**/Derived/
**/*.xcodeproj
**/*.xcworkspace
.build/
graph.png

# Xcode
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Swift Package Manager
.swiftpm/
# Commit Package.resolved files (lock SwiftLint and any future SPM deps).

# Build timing reports (local, not committed)
.build-reports/

# Claude Code (per-machine local settings)
.claude/settings.local.json

# Git worktrees
.worktrees/

# Xcode build artifacts (generated by xcodebuild outside DerivedData)
**/build/

# Local planning docs (not committed)
docs/
